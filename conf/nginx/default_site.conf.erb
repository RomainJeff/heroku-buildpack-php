# Default configuration for non-framework apps
root /app/<%= ENV['DOCUMENT_ROOT'] %>;

location ~ \.php {
    include fastcgi_params;
    fastcgi_param  SCRIPT_FILENAME    $document_root$fastcgi_script_name;
    fastcgi_pass php;
    fastcgi_buffers 256 4k;
}

location /accueil {
  rewrite ^/accueil(/)?$ /index.php break;
}

location /concept {
  rewrite ^/concept(/)?$ /concept.php break;
}

location /services {
  rewrite ^/services(/)?$ /services.php break;
}

location /tarifs {
  rewrite ^/tarifs(/)?$ /services.php break;
}

location /salons {
  rewrite ^/salons(/)?$ /salons.php break;
}

location /plans {
  rewrite ^/plans(/)?$ /salons.php break;
}

location /livredor {
  rewrite ^/livredor(/)?$ /livredor.php break;
}

location /ajoutlivredor {
  rewrite ^/ajoutlivredor(/)?$ /livredor_add.php break;
}

location /partenaires {
  rewrite ^/partenaires(/)?$ /partenaires.php break;
}

location /mentions {
  rewrite ^/mentions(/)?$ /mentions.php break;
}

location /presse {
  rewrite ^/presse(/)?$ /presse.php break;
}

location /articles {
  rewrite ^/articles(/)?$ /articles.php break;
}

location /ie {
  rewrite ^/ie(/)?$ /ie.php break;
}

location /manage {
  rewrite ^/manage(/)?$ /manage.php break;
}
